# App title (shown in sidebar/logo/tab title)
VITE_APP_TITLE=Autest

# App namespace — prefix for cache / store keys to keep them isolated
VITE_APP_NAMESPACE=autest

# Secret key used to encrypt the store when persisted to localStorage
VITE_APP_STORE_SECURE_KEY=autest-portal-store-key
